#
# Be sure to run `pod lib lint PesamoniObjc.podspec' to ensure this is a
# valid spec and remove all comments before submitting the spec.
#
# Any lines starting with a # are optional, but encouraged
#
# To learn more about a Podspec see http://guides.cocoapods.org/syntax/podspec.html
#

Pod::Spec.new do |s|
    s.name             = "PesamoniObjc"
    s.version          = "1.0.0"

    s.summary          = "Pesaway Pesamoni API Documentation"
    s.description      = <<-DESC
                         No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
                         DESC

    s.platform     = :ios, '7.0'
    s.requires_arc = true

    s.framework    = 'SystemConfiguration'

    s.homepage     = "https://github.com/Pesamoni/pesamoni_objc"
    s.license      = "Proprietary"
    s.source       = { :git => "https://github.com/Pesamoni/pesamoni_objc.git", :tag => "#{s.version}" }
    s.author       = { "Pesamoni Limited" => "inquiries@pesamoni.com" }

    s.source_files = 'PesamoniObjc/**/*.{m,h}'
    s.public_header_files = 'PesamoniObjc/**/*.h'


    s.dependency 'AFNetworking', '~> 3'
    s.dependency 'JSONModel', '~> 1.2'
    s.dependency 'ISO8601', '~> 0.6'
end

